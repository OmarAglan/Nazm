; حلقة.نظم — حلقة تعد من ١٠ إلى ٠ ثم تخرج بالرمز ٤٢
;
;   نظم --صيغة إلف64 --خرج حلقة.o examples/حلقة.نظم
;   ld -o حلقة -e البداية حلقة.o
;   ./حلقة
;   echo $?    ; النتيجة المتوقعة: ٤٢

.نص
.عام البداية

البداية:
    انقل سجل_العداد، ١٠       ; قيمة العداد الابتدائية

الحلقة:
    انقص سجل_العداد
    قارن سجل_العداد، ٠
    اقفز_غير_صفر الحلقة         ; كرر حتى يصبح العداد صفرا

    انقل سجل_المركم، ٦٠         ; نداء exit
    انقل فهرس_الوجهة، ٤٢        ; رمز الخروج
    ناد_النظام
