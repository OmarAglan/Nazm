; خروج.نظم — أصغر برنامج نَظْم قابل للربط على لينكس
;
;   نظم --صيغة إلف64 --خرج خروج.o examples/خروج.نظم
;   ld -o خروج -e البداية خروج.o
;   ./خروج
;   echo $?    ; النتيجة المتوقعة: ٠

.نص
.عام البداية

البداية:
    انقل سجل_المركم، ٦٠                 ; نداء exit
    خالف_بتيا فهرس_الوجهة، فهرس_الوجهة ; رمز الخروج = ٠
    ناد_النظام
