; بيانات.مجمع — مثال على قسم .بيانات
; يُعرّف ثوابت في .بيانات ثم يستخدمها
; تجميع: ./nazm examples/بيانات.مجمع -o بيانات.o
; ربط:   ld بيانات.o -e البداية -o بيانات
; تشغيل: ./بيانات ; echo $?   ← يجب أن يُظهر 7

.نص
.عام البداية

البداية:
    احمل ر0، ٦٠        ; exit syscall
    احمل ر7، 7         ; رمز الخروج = 7
    نداء_نظام

.بيانات

القيمة:    .عدد٣٢ 7
الرسالة:   .مساحة 16
السحر:     .عدد٦٤ 0xDEADBEEF
