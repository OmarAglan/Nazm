; خروج.مجمع — exit(0) على لينكس
; تجميع: ./nazm examples/خروج.مجمع -o خروج.o
; ربط:   ld خروج.o -o خروج
; تشغيل: ./خروج ; echo $?

.نص
.عام البداية

البداية:
    احمل ر0، ٦٠       ; رقم نداء النظام exit = 60
    خالف ر7، ر7       ; rdi = 0  (رمز الخروج)
    نداء_نظام          ; syscall
