; بيانات.نظم — التوجيهات العددية والسلسلة والحجز الصفري
;
;   نظم --صيغة إلف64 --خرج بيانات.o examples/بيانات.نظم
;   readelf -x .data -r بيانات.o
;
; يحمل عنوان الرسالة ليولد قيد ترحيل، ثم يخرج بالرمز ٧ على لينكس.

.نص
.عام البداية

البداية:
    انقل سجل_البيانات، الرسالة  ; عنوان بيانات يولد قيد ترحيل
    انقل سجل_المركم، ٦٠         ; نداء exit
    انقل فهرس_الوجهة، ٧         ; رمز الخروج
    ناد_النظام

.بيانات

بايتات:      .عدد٨ ١، ٢، ٢٥٥
كلمة:        .عدد١٦ 0x1234
عدد:         .عدد٣٢ 2026
عدد_عريض:    .عدد٦٤ 0xDEADBEEF
حجز:         .مساحة_صفرية ١٦
الرسالة:     .سلسلة_منتهية_بصفر "نَظْم\n"
