# سجل التغييرات

يتبع هذا الملف صيغة قريبة من Keep a Changelog، مع إبقاء اللغة عربية لأنها جزء من هوية المشروع.

## غير منشور

### أضيف

- إضافة `tests/unit/test_cli_args.c` لتغطية تحليل خيارات CLI، الصيغ، المسارات، وحالات الخطأ.
- إضافة اختبارات لإزاحات الذاكرة بالأرقام العربية-الهندية، بما في ذلك `[ر1+٨]` و`[ر5-١٦]`.
- إضافة حد 100 MiB لقراءة ملفات المصدر في CLI مع رسائل خطأ عربية مخصصة لحالات القراءة المختلفة.

### تغير

- تحديث `README.md` وملفات `Docs/` و`ROADMAP.md` لتطابق حالة البناء والاختبارات الحالية.
- تسجيل 12 مجموعة اختبارات وحدة في كل من CTest و`build.sh test`.
- توثيق أن `libnazm` يبنى من مصادر المكتبة فقط، وأن `src/main.c` يخص هدف CLI.

## 0.1.0 - 2026-05-24

### أضيف

- إنشاء بنية مشروع CMake الأساسية لمشروع نَظْم.
- إضافة مشغل CLI في `src/main.c` ينسق خط الأنابيب من قراءة الملف إلى كتابة الخرج.
- إضافة وحدات أساسية: `alloc`, `unicode`, `error`, `lexer`, `parser`, `symtable`, `passes`, `encoder`, `output`، و`cli`.
- إضافة واجهة عامة أولية في `include/nazm.h` للاستخدام المستقبلي من أدوات أخرى.
- إضافة اختبارات وحدة أولية لوحدات الذاكرة، Unicode، جدول الرموز، الكلمات المفتاحية، الفوريّات، وREX.
- إضافة وثائق تحليلية في `Docs/` تغطي البنية، المكدس التقني، الاختبارات، التكاملات، المخاوف، والأعراف.
- `6c402ef` - إضافة محلل معجمي كامل للمصدر العربي: تعليمات، سجلات مسماة ورقمية `ر0` إلى `ر15`، فوريّات ASCII وعربية-هندية، أعداد عشرية/سداسية/ثنائية، أعداد سالبة، توجيهات، وسوم، فواصل عربية وإنجليزية، تعليقات، وطي الأسطر الفارغة.
- `6c402ef` - إضافة `tests/unit/test_lexer.c` لتغطية المحلل المعجمي: التعليمات، السجلات، الفوريّات، الوسوم، التوجيهات، علامات الترقيم، التعليقات، تتبع الأسطر، وحالات الخطأ.
- `6c402ef` - إضافة `build.sh` كمسار بناء واختبار مباشر دون CMake.
- `6c402ef` - إضافة مثال عربي جديد في `examples/2.مجمع`.
- `7b64f98` - إضافة محلل نحوي كامل في `src/parser/parser.c` مع مؤشر توكنات، تحليل المعاملات، السجلات، الفوريّات، معاملات الذاكرة، الوسوم، التوجيهات، فحص عدد المعاملات، التعافي من الأخطاء، ونمو قائمة التعليمات.
- `7b64f98` - إضافة `tests/unit/test_parser.c` لتغطية التعليمات، المعاملات، الذاكرة، الوسوم، التوجيهات، حالات الخطأ، تتبع الأسطر، والبرامج متعددة الأسطر.
- `7b64f98` - تصدير `lexer_register_id()` في `src/lexer/lexer.h` حتى يستخدمه المحلل النحوي في حل أسماء السجلات.
- `7b64f98` - إضافة مثال عربي جديد في `examples/3.مجمع`.
- إضافة `AGENTS.md` كتعليمات تشغيل تفصيلية للوكيل البرمجي داخل مستودع نَظْم.
- إضافة `ROADMAP.md` بخطة مرحلية دقيقة ومقسمة إلى مهام صغيرة.
- توسيع `README.md` ليصبح دليلا عربيا كاملا يشرح الفكرة، الحالة، البناء، الاختبار، الاستخدام، وبنية المشروع.
- إضافة `LICENSE` برخصة MIT.
- إضافة `tools/check-markdown.ps1` للتحقق من روابط Markdown الداخلية والمسارات الموثقة.
- إنشاء هذا السجل لتوثيق التغييرات المستقبلية بطريقة منتظمة.

### تغير

- `6c402ef` - تحسين تصنيف الحروف العربية في `src/unicode/arabic.c` باستبعاد علامات الترقيم والأرقام من `is_arabic_letter`.
- `7b64f98` - تحديث `build.sh` لإضافة `test_parser` إلى قائمة اختبارات الوحدة.
- توحيد وصف المشروع حول كونه مُجمِّعا عربيا لبنية x86-64 مكتوبا بـ C11 وبلا اعتماديات تشغيل خارجية.
- تحديث `Docs/STRUCTURE.md` و`Docs/ARCHITECTURE.md` و`Docs/TESTING.md` لتفصل بين التنفيذ الحالي والعناصر المخططة قبل إصدار `0.1.0`.
