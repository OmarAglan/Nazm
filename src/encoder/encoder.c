/*
 * encoder/encoder.c
 * Entry point — all encoding logic lives in table.c.
 * This file intentionally left minimal.
 */
#include "encoder.h"
/* encoder_encode() and encoder_instruction_size() are defined in table.c */
